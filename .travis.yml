language: php

php:
  - 7.3

install:
  - composer selfupdate
  - composer install

script:
  - ./vendor/bin/phpunit --colors --coverage-text

#after_success:
  #- travis_retry php vendor/bin/coveralls